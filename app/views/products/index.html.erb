<h1>Your Cart:</h1>

<ul>

<% @cart.each do |item| %>
  <li>
    <%= item %>
  </li>
<% end %>

</ul>

<h3>Add an Item:</h3>
<%= form_for add_product_path do |f|%>
  <%= f.label :product %>
  <%= f.text_field :product %>
  <%= submit_tag "add to cart" %>
  <% end %>